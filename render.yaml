services:
  - type: web
    name: sso-hfag-teste
    runtime: docker
    plan: free
    healthCheckPath: /login
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sso-hfag-teste-db
          property: connectionString
      - key: SSO_ADMIN_EMAIL
        sync: false
      - key: SSO_ADMIN_PASSWORD
        sync: false

databases:
  - name: sso-hfag-teste-db
    databaseName: sso_hfag_teste
    plan: free
    ipAllowList: []
